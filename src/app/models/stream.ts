export interface ChannelStreamData {
    channel_name: string;
    stream_url: string;
    tvshow_title: string;
}

export interface VideoStreamData {
    is_start: boolean;
    is_tvzavr: boolean;
    name: string;
    stream_url: string;
}
